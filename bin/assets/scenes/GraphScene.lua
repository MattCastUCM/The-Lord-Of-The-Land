scene = {
    graph = {
        components = {
            Transform = {
                positionX = WINDOW_WIDTH *0.065 + WINDOW_WIDTH *0.25,
                positionY = WINDOW_HEIGHT*0.65,
            },
            DrawGraph = { }
        }
    },
}